import React, { Component, PropTypes } from 'react'

const EmailVerified = () => (
  <p>EmailVerified</p>
)

export default EmailVerified

// EmailVerified.propTypes = { children: React.PropTypes.string };
// EmailVerified.defaultProps = { children: 'Hello World!' };
